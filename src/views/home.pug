extends base.pug
include mixins/video

block content
    nav 
        ul
            li 
                a(href="/videos/upload") Upload Video
    
    ul
        each item in videos
            +video(item)
        else
            li Sorry nothing found.